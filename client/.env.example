# Optional when the API is hosted separately. The default is /api.
VITE_API_URL=/api
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Used only by the local Vite proxy.
VITE_DEV_API_TARGET=http://localhost:5001
